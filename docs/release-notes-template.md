# DaisyOS VERSION — CHANNEL

## Highlights

- Summarize the most important user-visible changes.

## Changes

- Added:
- Changed:
- Fixed:

## Known issues

- List verified limitations and affected environments.

## Verification

Download both the Linux x64 archive and its `.sha256` file, then run:

```bash
sha256sum --check DaisyOS-Shell-VERSION-CHANNEL-linux-x64.tar.gz.sha256
```

This release contains the self-contained DaisyOS shell, not an installable ISO.

